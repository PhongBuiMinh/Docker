# FROM - define which image to download and start from
FROM alpine:latest

# MAINTAINER - define the author who is creating the Dockerfile
MAINTAINER Phong Bui_Minh <phongyas05@gmail.com>

# RUN - is primarily used for building and configuring the container
RUN apk add g++ valgrind

# ADD - give instructions to copy new files, directories, or remote file URLs and then adds them to the filesystem of the image.
# ENV - set the environment variables both during the build and when running the result.
# ENTRYPOINT

# CMD ["echo", "Hello World!"]
WORKDIR /app
